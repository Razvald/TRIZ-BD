<Window x:Class="OnlineShop_MVVM_.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OnlineShop_MVVM_"
        xmlns:views="clr-namespace:OnlineShop_MVVM_.UserControls"
        xmlns:vm="clr-namespace:OnlineShop_MVVM_.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <ContentControl Margin="15" 
                    Content="{Binding CurrentViewModel}" 
                    HorizontalContentAlignment="Stretch" 
                    VerticalContentAlignment="Stretch">
        <ContentControl.Resources>
            <DataTemplate DataType="{x:Type vm:LoginVM}">
                <views:LoginView/>
            </DataTemplate>
        </ContentControl.Resources>
    </ContentControl>

</Window>
